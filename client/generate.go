package main

// Regenerate the API documentation (docs/openapi.json and docs/API.md) from the
// route registry in internal/apispec. Run with `go generate ./...` from the
// client/ directory.
//go:generate go run ./cmd/apidocgen -out ./docs
