{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Go: Test in Docker (unit)",
      "detail": "Run the Client Go unit tests inside a golang container (offline, no server).",
      "type": "shell",
      "command": "python",
      "args": ["dev.py", "docker"],
      "options": { "cwd": "${workspaceFolder}" },
      "group": {
        "kind": "test",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true
      },
      "problemMatcher": ["$go"]
    },
    {
      "label": "Go: Live tests in Docker (real server)",
      "detail": "Run the live-tagged integration tests in Docker against BLENDKIT_SERVER, piping credentials from .env (API_KEY -> BLENDKIT_API_KEY).",
      "type": "shell",
      "command": "python",
      "args": ["dev.py", "docker", "--live"],
      "options": { "cwd": "${workspaceFolder}" },
      "group": "test",
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true
      },
      "problemMatcher": ["$go"]
    }
  ]
}
